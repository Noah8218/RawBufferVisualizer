# Visual Studio 2022 17.9 Compatibility Qualification

Last verified: 2026-08-06 KST.

This is the immutable historical record for the 2,491,491-byte v4 architecture candidate. The later frozen 2.0.2 release package, PL-0001 repair, and full exact-package matrix are recorded separately in [release-qualification-2.0.2.md](release-qualification-2.0.2.md). Do not use the v4 VSIX for publication.

## Goal And Boundary

This isolated branch proves that the current 2.x hybrid extension can support Visual Studio 2022 `17.9+` without regressing the already-qualified stable Visual Studio 2026 path.

- Branch: `agent/vs2022-17.9-compat`
- Baseline commit: `27f868d`
- Development version: `2.0.2` / VSIX identity version `2.0.2.0`
- VSSDK package: `net472`, `Microsoft.VisualStudio.SDK 17.9.37000`, `Microsoft.VSSDK.BuildTools 17.9.3184`
- Out-of-process provider: `net8.0-windows8.0`, `Microsoft.VisualStudio.Extensibility.Sdk/Build 17.9.2092`
- Declared VS2022 target: `[17.9,18.0)`, x64 Community/Professional/Enterprise
- Declared CoreEditor prerequisite: `[17.9,)`
- Stable VS2026 remains a compatibility target through its Visual Studio API 17.x compatibility behavior.

Public Marketplace `2.0.1.0` still has a `17.14+` floor. This document qualifies the unpublished development architecture and exact candidate; it does not authorize publication or retroactively change the public package.

## Exact Qualified Development Candidate

```text
Path: D:\OpenVisionLab-TestData\RawBufferVisualizer\vs2022-17.9-runtime-qualification-20260806\candidate-v4\RawBufferVisualizer-VisualStudioExtensibility-net472\RawBufferVisualizer.VisualStudio.Extensibility.vsix
Manifest version: 2.0.2.0
Size: 2,491,491 bytes
SHA-256: 36EEE192AE508B5C2C9C6DCCB55EB1F19E5A0AD976E56F65414A36C8BF32BDAF
Marketplace extension ID: RawBufferVisualizer.34f8ad30-2f11-4c37-a9d4-00f3a8c1d29f
Package entries: 72
```

Do not substitute the later local rebuild. All 72 payload entries are semantically identical except the two generated installer metadata files use a new random `extensionDir`, but the ZIP bytes and SHA-256 differ. Runtime qualification belongs to the exact candidate above.

## Architecture Result

- `RawBufferVisualizer.VisualStudio.Vssdk` now owns the in-process `AsyncPackage`, VSCT, generated `.pkgdef`, commands, debugger events, ToolWindow, and VSIX container.
- `RawBufferVisualizer.VisualStudio.Extensibility` now owns only the out-of-process debugger visualizer providers and is packaged under `OutOfProc`.
- The Classic project remains excluded from production packaging.
- Package GUIDs, command IDs, ToolWindow identity, supported object registrations, transfer protocol, image rendering, and user workflow remain unchanged.
- The optional Visual Studio extension-development workload is not a runtime requirement.

## Exact Installed Runtime Matrix

| Host | Exact candidate checks | Result |
| --- | --- | --- |
| VS2022 Professional `17.9.34902.65` | EnvironmentCheck, AutomaticCollections, MultiLibraryHybrid | Pass |
| VS2022 Professional `17.9.34902.65` without extension-development workload | MultiLibraryHybrid | Pass |
| VS2022 Community `17.14.37516.0` | MultiLibraryHybrid regression | Pass |
| VS2026 Community `18.8.12023.21` | MultiLibraryHybrid regression | Pass |

The final exact-v4 17.9 EnvironmentCheck reported the supported host, loaded `2.0.2.0` extension, writable D-drive temporary storage, repeated-click panel close, one Open command, one Scan command, and zero package-protocol errors. AutomaticCollections opened five valid collection items, isolated two intended invalid items, produced no duplicates, restored the saved preference, and reported zero package-protocol errors. MultiLibraryHybrid opened nine documents with zero product errors and exercised registered Bitmap handoff plus automatic inspection.

All desktop EXE runs used the dynamically selected leftmost monitor `\\.\DISPLAY2`, bounds `Left=-1920`, `Top=365`, `Width=1920`, `Height=1080`; the verified Visual Studio window rectangle was `Left=-1900`, `Top=385`, `Right=-20`, `Bottom=1425`.

## Source, Package, And Restore Verification

- `dotnet restore RawBufferVisualizer.sln` passed.
- Release solution build passed with zero errors and 18 pre-existing `VSTHRD010` warnings in `ImageTypeRecognizer.cs`.
- `RawBufferVisualizer.Tests` passed, including acceptance of 17.9/17.14/18.x host text and rejection of 17.8.
- Fourteen product assemblies matched the exact candidate, the current Release build, the restored VS2022 installation, and the restored VS2026 installation: 42 comparisons, 42 matches, zero mismatches.
- A full rebuild contained the same 72 payload entries. Seventy were byte-identical; `catalog.json` and `manifest.json` differed only in the generated random installer `extensionDir`.
- The test-only Professional 17.9 instance was removed after qualification.
- VS2022 Community `17.14.37516.0` and VS2026 Community `18.8.12023.21` remain complete, launchable, not pending reboot, and retain the exact candidate installation.

## Evidence

Root:

```text
D:\OpenVisionLab-TestData\RawBufferVisualizer\vs2022-17.9-runtime-qualification-20260806
```

Key records:

- `smoke\professional-17.9-v4-final-environment\EnvironmentCheck-installed-vsix.json`
- `smoke\professional-17.9-v4-final-automatic-collections\AutomaticCollections-installed-vsix.json`
- `smoke\professional-17.9-v4-explicit-menu-v2\MultiLibraryHybrid-installed-vsix.json`
- `smoke\professional-17.9-v4-no-extension-workload-v2\MultiLibraryHybrid-installed-vsix.json`
- `smoke\community-17.14-v4-regression-v4\MultiLibraryHybrid-installed-vsix.json`
- `smoke\community-18.8-v4-regression\MultiLibraryHybrid-installed-vsix.json`
- `final-validation\candidate-source-install-equality.json`
- `final-validation\candidate-rebuild-entry-equality.json`
- `final-validation\release-build.log`
- `final-validation\aggregate-self-tests.log`
- `final-validation\vs-instances-after-final-v4-scenarios.json`

## Historical Boundary And Subsequent Release Gate

At this candidate's 2026-08-06 checkpoint, PL-0001 recorded a separate edge defect: a deliberately long `TEMP` root could make the generated `.processing.<guid>` handoff path exceed 259 characters in the `net472` host, causing the claim to be skipped. The same exact candidate passed with a shorter D-drive `TEMP` root, proving this was not a Visual Studio workload dependency.

This exact development VSIX must not be uploaded because its embedded release communication still describes 2.0.1. The later 2.0.2 release-preparation work completed the first three gates below and is recorded in [release-qualification-2.0.2.md](release-qualification-2.0.2.md):

1. Update the release announcement, embedded notes, changelog, README, Marketplace Overview, and release notes to the approved 2.0.2 message.
2. Freeze a new exact package and record its size and SHA-256.
3. Repeat the installed EnvironmentCheck, AutomaticCollections, MultiLibraryHybrid, package-equality, menu-count, and protocol checks on real VS2022 17.9, current serviced VS2022, and stable VS2026.
4. Commit, push, CI, Marketplace upload, and public readback remain separate owner-authorized actions.

## Durable Checkpoint

Status: Complete

Scope: Development-only 2.0.2 compatibility architecture and exact candidate qualification for VS2022 17.9+, with current VS2022 and VS2026 regressions and environment restoration.

Acceptance criteria: source/version/install declarations aligned to 17.9 -> pass; unit/build/package checks -> pass; exact candidate on real stable VS2022 17.9 -> pass; optional extension-development workload absent -> pass; current VS2022 and VS2026 regressions -> pass; machine restored -> pass.

Verification: exact installed scenarios, build/self-tests, 42/42 assembly comparisons, semantic 72-entry rebuild comparison, and final `vswhere` state listed above.

Evidence: exact candidate path/hash and D-drive records listed above; PL-0001 is the durable record for the long-TEMP edge defect.

Boundary / next dependency: This completes compatibility qualification, not release publication. Any release-communication or package-content change creates a new candidate and requires a fresh exact-package matrix plus explicit owner approval before commit, push, or Marketplace upload.
