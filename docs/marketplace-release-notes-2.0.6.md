# Raw Buffer Visualizer 2.0.6

Version `2.0.6` advances the Marketplace package identity because `2.0.5.0` is already public and published versions are immutable.

## Package Update

- Advances the extension and assembly version to `2.0.6`/`2.0.6.0`.
- Makes no functional or compatibility change to the validated 2.0.5 behavior.
- Retains exact debugger expression names, native `Ptr` versus `Pixels` provenance, complete image-array registration, fail-closed pointer reads, and negative-stride Bitmap correction.

**Clear all**, direct concurrent dictionaries, first-use `ImagePtr` handoff, Visual Studio 2022 `17.9+`, and stable Visual Studio 2026 `18.x` support remain included. After installing or updating, close every Visual Studio window and restart Visual Studio.
